[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "autonomy-stack-simulator"
version = "0.1.0"
description = "GNC Simulation Stack"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]