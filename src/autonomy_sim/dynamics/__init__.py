# dynamics: owns vehicle dynamics models.
# potential dynamics models: 
# - simple kinematic point mass (x, y, vx, vy) with control input (ax, ay) [double integrator]
# - single integrator (x, y) with control input (vx, vy)
# - unicycle model (x, y, theta, speed) with control input (a, omega)
# - bicycle model (x, y, theta) with control input (a, steering angle)

# first - implement point mass
